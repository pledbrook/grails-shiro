language: groovy
env:
  - TERM: dumb
jdk:
  - oraclejdk7
  - oraclejdk8
before_install:
  - cd ./shiro3
  - chmod +x ./gradlew
script: ./gradlew -q clean :grails-shiro:test :cli-tests:test :wildcard-realm:integrationTest --stacktrace

